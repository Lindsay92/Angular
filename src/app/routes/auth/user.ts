export class User {

    constructor(
        public firstName = '',
        public lastName = '',
        public email = '',
        public password = ''
        ) {}
}

//create a new instance of this class to the new user information so it is not an interface